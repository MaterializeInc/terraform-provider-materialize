version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

  exclusions:
    rules:
      # Test helpers intentionally ignore some setup errors.
      - path: _test\.go
        linters:
          - errcheck
